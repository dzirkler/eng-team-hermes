name: ux-designer
model: glm-5.2
toolsets:
  - read
  - edit    # intended scope: design-brief paths only (specs/NNN-*/design-
            # brief.md, docs/style-reference.md) — same path-scoping caveat
            # as quality-engineer; verify enforcement during task #6.
  - write
  - kanban_comment
# No bash. No kanban_block.
